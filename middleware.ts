export { default } from "next-auth/middleware";

export const config = {
  matcher: [
    // "/collections/new",
    // "/collections/:id",
    // "/categories/new",
    // "/categories/:id",
    // "/saved",
    // "/profile/:path*",
  ],
};
